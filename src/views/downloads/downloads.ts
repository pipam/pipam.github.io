export const version = '1.2.0';
export const description = "Adds a list cache (only per session) feature to Pipam so you don't have to constantly wait for the list to be refetched. If the cache is stale (i.e. out of date, needs to be updated), just hit the `Refresh` button.";
